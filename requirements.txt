# Tape Drive Controller
# Python 3.9+

# GTK 3 bindings (PyGObject). On Linux Mint MATE install system packages:
#   sudo apt install python3-gi python3-gi-cairo gir1.2-gtk-3.0
# Then: pip install PyGObject  (or use system python3-gi only)
PyGObject>=3.42.0

# For development and testing
pytest>=7.0
